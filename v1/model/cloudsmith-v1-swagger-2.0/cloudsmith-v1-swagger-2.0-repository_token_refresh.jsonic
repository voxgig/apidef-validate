# Entity: repository_token_refresh

main: sdk: entity: repository_token_refresh: {

  alias: field: {}

  id: {
    field: "id"
    name: "id"
  }

  name: "repository_token_refresh"
  op: {
    create: {
      alts: [
        {
          args: {
            param: [
              {
                kind: "param"
                name: "owner"
                req: true
                type: "`$ANY`"
              }
              {
                kind: "param"
                name: "repo"
                req: true
                type: "`$ANY`"
              }
            ]
            query: [
              {
                kind: "query"
                name: "data"
                req: false
                type: "`$OBJECT`"
              }
              {
                kind: "query"
                name: "show_token"
                req: false
                type: "`$ANY`"
              }
            ]
          }
          method: "POST"
          orig: "/entitlements/{owner}/{repo}/{identifier}/refresh/"
          parts: [
            "entitlements"
            "{owner}"
            "{repo}"
            "{identifier}"
            "refresh"
          ]
          select: {
            param: {
              identifier: true
              owner: true
              repo: true
            }
          }
        }
      ]
      name: "create"
    }
  }

  relations: {
    ancestors: [
      [
        "entitlements"
        "{owner}"
        "{repo}"
      ]
    ]
  }



}