# Entity: webhook

main: kit: entity: webhook: {

  alias: field: {}

  id: {
    field: "id"
    name: "id"
  }

  name: "webhook"
  op: {
    remove: {
      name: "remove"
      targets: [
        {
          args: {
            params: [
              {
                kind: "param"
                name: "identifier"
                orig: "identifier"
                reqd: true
                type: "`$ANY`"
              }
              {
                kind: "param"
                name: "owner"
                orig: "owner"
                reqd: true
                type: "`$ANY`"
              }
              {
                kind: "param"
                name: "repo"
                orig: "repo"
                reqd: true
                type: "`$ANY`"
              }
            ]
          }
          method: "DELETE"
          orig: "/webhooks/{owner}/{repo}/{identifier}/"
          parts: [
            "webhooks"
            "{owner}"
            "{repo}"
            "{identifier}"
          ]
          select: {
            exist: [
              "identifier"
              "owner"
              "repo"
            ]
          }
          transform: {
            req: "`reqdata`"
            res: "`body`"
          }
        }
      ]
    }
  }

  relations: {
    ancestors: [
      [
        "webhooks"
        "{owner}"
      ]
      [
        "webhooks"
        "{owner}"
        "{repo}"
      ]
    ]
  }



}