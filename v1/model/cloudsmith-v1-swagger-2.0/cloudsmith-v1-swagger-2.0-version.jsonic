# Entity: version

main: kit: entity: version: {

  alias: field: {}

  id: {
    field: "id"
    name: "id"
  }

  name: "version"
  relations: {
    ancestors: [
      [
        "version"
        "{owner}"
        "{repo}"
        "{package_format}"
        "{package_name}"
        "{package_version}"
      ]
    ]
  }



}