# Entity: view

main: kit: entity: view: {

  alias: field: {}

  fields: [
    {
      name: "count"
      req: true
      type: "`$INTEGER`"
    }
    {
      name: "timestamp"
      req: true
      type: "`$STRING`"
    }
    {
      name: "unique"
      req: true
      type: "`$INTEGER`"
    }
  ]

  id: {
    field: "id"
    name: "id"
  }

  name: "view"
  op: {
    list: {
      name: "list"
      points: [
        {
          args: {
            params: [
              {
                kind: "param"
                name: "owner"
                orig: "owner"
                reqd: true
                type: "`$STRING`"
              }
              {
                kind: "param"
                name: "repo"
                orig: "repo"
                reqd: true
                type: "`$STRING`"
              }
            ]
            query: [
              {
                kind: "query"
                name: "per"
                orig: "per"
                reqd: false
                type: "`$STRING`"
              }
            ]
          }
          method: "GET"
          orig: "/repos/{owner}/{repo}/traffic/views"
          parts: [
            "repos"
            "{owner}"
            "{repo}"
            "traffic"
            "views"
          ]
          select: {
            exist: [
              "owner"
              "per"
              "repo"
            ]
          }
          transform: {
            req: "`reqdata`"
            res: "`body`"
          }
        }
      ]
    }
  }

  relations: {
    ancestors: [
      [
        "repos"
        "{owner}"
      ]
    ]
  }



}